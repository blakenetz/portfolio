---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"div"> {}

const { class: className, ...props } = Astro.props;
---

<div
  {...props}
  class:list={["grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4", className]}
>
  <slot />
</div>
