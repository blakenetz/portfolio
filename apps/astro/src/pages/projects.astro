---
import GitHubProjectsGrid from "~/components/projects/GitHubProjectsGrid.astro";
import Grid from "~/components/projects/Grid.astro";
import ProjectsOverviewGrid from "~/components/projects/ProjectsOverviewGrid.astro";
import Layout from "~/layouts/Root.astro";
import { getRepos } from "~/lib/projects.server";
import { status } from "~/lib/utils";
import { ANIMATE_COLORS_CLASSES } from "~/consts";
import SectionHeader from "~/components/projects/SectionHeader.astro";
const repos = await getRepos(Astro.request);

// nothing to show :( redirect
if (repos.every((r) => r.status === 400)) {
  return redirect(`/?status=${status.octokit}`);
}

const personal = repos[0];
const work = repos[1];
---

<style>
  .text-clamp {
    font-size: clamp(2rem, 33vh, 20vw);
    line-height: 1;
  }
</style>

<Layout
  headProps={{
    title: "BN | Projects",
    description: "A selection of my GitHub projects",
  }}
  layoutProps={{ showNav: true }}
>
  <div class="flex flex-col gap-8 py-8">
    <section>
      <SectionHeader>
        <h1 class={`${ANIMATE_COLORS_CLASSES} text-clamp`}>Projects</h1>
      </SectionHeader>
      <ProjectsOverviewGrid server:defer>
        <Grid slot="fallback" class="animate-pulse">
          <div class="bg-dodger-blue-900/50 rounded-xs h-[500px] w-full"></div>
          <div class="bg-dodger-blue-800/50 rounded-xs h-[500px] w-full"></div>
          <div class="bg-dodger-blue-700/50 rounded-xs h-[500px] w-full"></div>
          <div class="bg-dodger-blue-600/50 rounded-xs h-[500px] w-full"></div>
          <div class="bg-dodger-blue-500/50 rounded-xs h-[500px] w-full"></div>
          <div class="bg-dodger-blue-400/50 rounded-xs h-[500px] w-full"></div>
          <div class="bg-dodger-blue-300/50 rounded-xs h-[500px] w-full"></div>
          <div class="bg-dodger-blue-200/50 rounded-xs h-[500px] w-full"></div>
        </Grid>
      </ProjectsOverviewGrid>
    </section>
    <section>
      <SectionHeader>
        <h2>
          <span class={`${ANIMATE_COLORS_CLASSES} text-clamp`}>Personal</span> GitHub
          Projects
        </h2>
      </SectionHeader>
      <GitHubProjectsGrid data={personal.data} server:defer name="personal">
        <Grid slot="fallback" class="animate-pulse">
          <div class="rounded-xs h-[200px] w-full bg-rose-900/50"></div>
          <div class="rounded-xs h-[200px] w-full bg-rose-800/50"></div>
          <div class="rounded-xs h-[200px] w-full bg-rose-700/50"></div>
          <div class="rounded-xs h-[200px] w-full bg-rose-600/50"></div>
          <div class="rounded-xs h-[200px] w-full bg-rose-500/50"></div>
          <div class="rounded-xs h-[200px] w-full bg-rose-400/50"></div>
          <div class="rounded-xs h-[200px] w-full bg-rose-300/50"></div>
          <div class="rounded-xs h-[200px] w-full bg-rose-200/50"></div>
        </Grid>
      </GitHubProjectsGrid>
    </section>
  </div>
  <section>
    <SectionHeader>
      <h2>
        <span class={`${ANIMATE_COLORS_CLASSES} text-clamp`}>Work</span> GitHub Projects
      </h2>
      <p class="text-sm">
        The following are typically CI items, POC, etc. and are aren't suited
        for production
      </p>
    </SectionHeader>
    <GitHubProjectsGrid data={work.data} server:defer name="work">
      <Grid slot="fallback" class="animate-pulse">
        <div class="rounded-xs bg-aquamarine-900/50 h-[200px] w-full"></div>
        <div class="rounded-xs bg-aquamarine-800/50 h-[200px] w-full"></div>
        <div class="rounded-xs bg-aquamarine-700/50 h-[200px] w-full"></div>
        <div class="rounded-xs bg-aquamarine-600/50 h-[200px] w-full"></div>
        <div class="rounded-xs bg-aquamarine-500/50 h-[200px] w-full"></div>
        <div class="rounded-xs bg-aquamarine-400/50 h-[200px] w-full"></div>
        <div class="rounded-xs bg-aquamarine-300/50 h-[200px] w-full"></div>
        <div class="rounded-xs bg-aquamarine-200/50 h-[200px] w-full"></div>
      </Grid>
    </GitHubProjectsGrid>
  </section>
</Layout>
